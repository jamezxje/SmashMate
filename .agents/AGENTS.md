# SmashMate Project Rules

## Git Branching Policy
- All implementation tasks, features, bug fixes, commits, and pushes MUST take place on the `develop` branch.
- DO NOT commit or push directly to the `main` branch.
- Before committing or pushing, ensure the active branch is `develop`.
