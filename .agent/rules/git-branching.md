---
description: Development branch rule — all code changes and commits must occur on develop branch
alwaysApply: true
---

# Git Branching Rule

- All development work, task implementations, commits, and pushes MUST be made on the `develop` branch.
- NEVER commit or push directly to the `main` branch.
